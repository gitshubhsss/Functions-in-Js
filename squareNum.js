//for a given array of numbers print the square of each value using for each loop
let arr = [1, 2, 3, 4, 5];
let calculateSquare = (value) => {
  square = value * value;
  console.log(square);
};
arr.map(calculateSquare);
